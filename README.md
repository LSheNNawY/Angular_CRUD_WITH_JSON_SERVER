# Small SPA Angular CRUD App. With Json server && Tailwind CSS

## Angular features
* Routing
* Services
* Validation
* Piping

## How to start
* Run npm i
* Run json-server --watch db.json [--port yourPort]
* Run `ng serve -o` for a dev server
